# Gitlab项目管理

```markdown
# 协同机制
# 开发、测试、运维三个部门的关系
- 开发、测试、运维不是三个独立部门，他们相互紧密联系，但又相互制约
- 开发只负责写程序，将运行无误的程序提交至版本库中
- 开发不能私自将程序交给运维部署，也不能将编译好的程序给测试人员
- 测试部只能从版本库提取代码，然后编译，打包，运行，测试
- 不允许测试部将代码交给运维部部署
- 避免代码没有经过版本库流入生产环境，造成线下与线上代码不一致
- 运维部负责部署应用程序，配置管理，只接受测试部确认无误的版本，部署代码只能从版本库中获取
```

## 1. 标记定义

> 主标记、项目标记

```txt
怎样用好标记？
群组标记：公共标记定义在群组中，例如：
    任务，测试，BUG,升级，合并，重复
项目标记：个性标记定义在项目中，例如：
    项目XXX标记：需求文档，设计文档，产品说明书
    项目XXX标记：CSS,H5,JS
    项目XXX标记：数据库，短信，缓存，中间件，网关，队列等
群组+项目组合使用：升级+数据库，BUG+JS
```

## 2. 工作流
```txt
工作流设计原则:
    1. 遵循瀑减法原则而不是加法原则，参与人越少越好，节点越少越好。
    2. 尽量线性，从一端流向另一端，中间尽量不出现分叉
    3. 尽可能不出现逻辑判断分叉，例如A审批决定下一步是流向B还是C
    4. 以免循环，依赖关系以免循环，即流程后退
目的是让工作流可操作易操作，能冗余
```

## 3. 里程碑
项目节点

## 4. 议题

> 每个任务都会对应一个议题
```txt
任务分解
1. 尽可能解耦
2. 交叉功能合并
3. 理清依赖关系
```

### 怎样写议题
```txt
1. 精确描述任务
2. 不实用模糊词汇，避免造成误解
3. 5W2H任务分配法则
 - What:做什么事？
 - Wy:为什么做这件事？有什么意义？目的是什么？有必要吗？
 - When:什么时候做，完成的时间是否适当？
 - Where:在什么地方做，在什么范围内完成？
 - Who:由谁负责做？由谁负责执行？谁更合适？熟练程度低的人能做吗？
 - HoW:怎样做
 - How much:成本（不是所有岗位都会涉及成本）
```

```markdown
# 一些示例
## 举例，运维任务
    - What: 为api服务器做负载均衡，多增加一个节点，负载均衡算法采用最小连接数。
    - Why: 目前api服务器只有一台，如果出现故障将影响到所有业务运行，顾该服务器存在单点故障，需要增加节点。
    - When: 本周内完成，周末上线。（此处可以写日期）
    - Where: 在A机柜，低2机位处，连接倒交换机第三个端口。
    - Who: X负责网络配置，X负责上架，X负责验收测试
    - How: 增加/etc/hosts设置如下
    api.example.com 127.0.0.1
    api1.example.com 192.168.2.5
    api2.example.com 192.168.2.6

## 举例，开发任务
 - What: 增加图片验证码。
 - Wy: 目前用户注册登陆以及发帖无验证吗，某些用户通过机器人软件批量开户/发广告帖，给管理带来很大困扰。
 - When: 2014-06-15开始开发，2014-06-2012：00上线。
 - Where: 用户注册，登陆与发帖处增加该功能，。
 - Wo: 张三负责验证码生成类的开发，李四负责用户注册，登陆U修改，王五负责发帖U的修改。
 - How: 具体怎么操作的细节，此处省略200字.

## 举例，测试任务
 - What: 测出X软件并发性能。
 - Wy: 目前XX软件在线任务达到200后，用户反映速度慢，经常掉线。
 - When: 故障时间点10：00AM,需要周二完成测试，周五完成优化，月底上线。（此处可以写日期）
 - Where: 在AAA分支检出代码，编译后部署到BBB环境。
 - Who: X负责网络配置，X负责软件部署，XX负责测试
 - How: 具体怎么操作的细节，此处省略200字.…

## 举例，促销任务
 - What: X产品促销。
 - Why: 目前XXX产品在X市场占有率X用户反映X。
 - When: 促销起始时间X结束时间XX
 - Where: AAA细分市场，BBB区域。
 - Who: XXX负责X,XX负责X,XX负责XX
 - How: 具体怎么操作的细节，此处省略200字…
 - How  much:成本XXX
```

## 5. Release Notes
```txt
Release Notes项目升级时，需要写一个文档纪录这次变动
1.内容包括
2.新增了什么
3.更改了什么
4.修复了什么
5.未解决得问题
6.改善了什么
7.忽略了什么
```

> 此外，还有changelog，用于写更详细的升级日志
